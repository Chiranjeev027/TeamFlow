# ============================================
# TeamFlow Backend Environment Configuration
# ============================================
# SECURITY WARNING: Never commit actual .env files to Git!
# This is a template file. Copy it to .env and fill in your actual values.

# ============================================
# Server Configuration
# ============================================
PORT=5000
NODE_ENV=development

# ============================================
# MongoDB Database Configuration
# ============================================
# Get your connection string from MongoDB Atlas:
# 1. Go to your cluster → Connect → Connect your application
# 2. Copy the connection string and replace <username>, <password>, and database name
# Example: mongodb+srv://myuser:mypassword@cluster0.xxxxx.mongodb.net/teamflow?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<USERNAME>:<PASSWORD>@<CLUSTER>.mongodb.net/teamflow?retryWrites=true&w=majority

# ============================================
# JWT Authentication Secret
# ============================================
# CRITICAL: Generate a strong, random secret for production!
# Generate a secure secret using one of these methods:
#   Node.js: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#   OpenSSL: openssl rand -hex 64
#   Online: https://generate-secret.vercel.app/64
JWT_SECRET=GENERATE_A_STRONG_RANDOM_SECRET_HERE_MINIMUM_32_CHARACTERS

# ============================================
# CORS Configuration
# ============================================
# Frontend URL for CORS (update for production)
# Development: http://localhost:3000
# Production: https://your-frontend-domain.com
CLIENT_URL=http://localhost:3000
